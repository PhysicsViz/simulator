import { defineConfig } from 'vite';

export default defineConfig({
  // Relative asset URLs make the built app work from GitHub Pages project paths,
  // custom domains, local file previews, and nested static hosting.
  base: './'
});
